# Tuples (for tuples we use Parenthesis)

numbers = (1, 2, 3, 4, 3 , 5, 6, 8, 3, 5)
print(numbers)
print("3's in tuple = " + str(numbers.count(3)))

numbers = (1, 2, 3, 4, 3, 9, 5, 6, 8, 3, 5)
print("tuple in index 5 = " + str(numbers.index(5)))

numbers = (1, 2, 3, 4, 3 , 5, 6, 8, 3, 5)
print(numbers)



